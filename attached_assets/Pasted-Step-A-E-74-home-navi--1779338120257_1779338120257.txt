Step A〜E の構造実装は完了確認できました。
74件のhome-navi型エラーは既存問題と判明したので、別タスクとして扱います (今は触らない)。

次に、実装した新機能が実機で動作するかを確認したい。
以下5つのチェックを順番に実行し、結果を報告してください。

## 事前確認: ビルドと起動

```bash
pnpm build 2>&1 | tail -20
```

build が成功するか確認 (型エラーは出ても build 自体は通るはず)。
build が失敗する場合、その時点で停止して報告。

build が通ったら開発サーバーを起動:
```bash
pnpm dev
```

エラーなく起動するか確認。

## チェック1: マスタCRUD

1. ブラウザで `/service-types` を開く
2. シードされた17件が一覧表示されているか
3. 「新規追加」で「テスト身体介護25分」を追加 (category: body, duration: 30)
4. 一覧に表示されることを確認
5. 編集して名前変更、削除も試す
6. `/shift-types` を開いて 5件のシード (明/早/日/遅/夜) が見えるか

報告: 各操作 OK / 失敗、失敗時のエラー内容

## チェック2: シフトと利用者契約

### シフト登録
1. `/shifts` を開く
2. 月カレンダーUI または同等のUIが表示されるか
3. 任意のスタッフ × 任意の日付セルをクリック
4. シフト種別を選んで割り当てできるか
5. 「2026/6/29(月)」 で:
   - 任意のスタッフを「明」に割当
   - 別のスタッフを「早」に割当
   - 別のスタッフを「日」に割当

### 利用者契約登録
1. 任意の利用者の詳細ページを開く
2. 「契約サービス」セクションが表示されるか
3. 「+ 新規サービス契約」を追加:
   - サービス種別: 任意 (例: 身体介護20分)
   - 曜日: 月・水・金
   - 開始時刻: 07:00
4. 一覧に反映されるか

報告: 各操作 OK / 失敗、UI の表示の正しさ

## チェック3: テンプレ動的合成 (最重要)

1. `/route-sheet-templates` を開く
2. 7曜日のカードグリッドが表示されるか
3. 月曜カードをクリック (インラインエディタ展開)
4. 「明」シフト種別の行を追加
5. その行に、チェック2で作成した契約サービスを配置
6. 保存
7. `/route-sheet?date=2026-06-29` (月曜) を開く
8. 上部に「テンプレート表示中(月曜テンプレ)」のバナーが出るか
9. ガント上に、明枠の行と訪問コマが表示されるか
10. 「この日を編集する」ボタンを押す
11. バナーが「当日シート表示中」に変わるか
12. 訪問コマをドラッグして時刻変更
13. 「テンプレートにリセット」ボタンを押す
14. 確認ダイアログ後、再度テンプレ表示に戻るか

報告: 各ステップを通過したか、詰まった場合は具体的な状況

## チェック4: 退去時のサービス自動終了

1. チェック2で契約サービスを登録した利用者の編集画面を開く
2. movedOutAt に 2026-07-01 をセット
3. movedOutReason に「テスト退去」と入力
4. 保存
5. DB直接確認:
```bash
psql "$DATABASE_URL" -c "SELECT id, resident_id, terminated_at, termination_reason FROM resident_services WHERE resident_id = '<該当利用者ID>';"
```
6. terminated_at が 2026-07-01、termination_reason が「テスト退去」になっているか
7. `/route-sheet?date=2026-07-06` (退去後の月曜) を開く
8. その利用者の訪問が消えているか

報告: DB値が正しいか、テンプレからフィルタされているか

## チェック5: 競合検出

1. `/route-sheet?date=2026-06-30` (火曜) を開く
2. 任意のスタッフ行に時刻 09:00-09:30 の訪問を追加
3. 同じスタッフ行に時刻 09:15-09:45 の訪問を追加 (重複)
4. 画面上部に競合バナーが出るか
5. 重複している2つのセルに赤枠が出るか
6. 1つの訪問を別時刻に移動 → 競合バナーが消えるか

報告: バナー表示、赤枠表示、解消検知、それぞれ OK / NG

## 報告フォーマット

各チェックについて、以下のテンプレで報告:

### チェック1: マスタCRUD
- 結果: 全部OK / 一部NG / 全部NG
- 詳細: (個別の操作結果)
- スクリーンショット: (あれば)

### チェック2: シフトと利用者契約
- 結果:
- 詳細:

### チェック3: テンプレ動的合成
- 結果:
- 通過ステップ: 1-X まで通過、X+1 で詰まる
- エラー内容:

### チェック4: 退去時自動終了
- 結果:
- DB値の確認結果:

### チェック5: 競合検出
- 結果:
- 詳細:

### 総合判定
全機能動作: Yes / No (Noの場合、どの機能がどこで詰まっているか)