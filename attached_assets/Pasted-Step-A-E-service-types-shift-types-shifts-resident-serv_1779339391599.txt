Step A〜E (service_types, shift_types, shifts, resident_services, route_sheet_templates) の実装は完了済み。
動作確認の結果、3件のバグが残っているため修正してほしい。

優先度順に B2 → B1 → UI3 で進めること。各修正後に typecheck と該当機能の動作確認を行うこと。

## 修正B2: Resident スキーマに退去・入院フィールドを追加 (最重要)

### Step 1: OpenAPI 仕様の修正

`lib/api-spec/openapi.yaml` を開き、`Resident` および `UpdateResidentBody` (または相当するUpdate系スキーマ) を探す。

両方のプロパティに以下を追加 (既にあればスキップ、なければ追加):

```yaml
Resident:
  type: object
  properties:
    # 既存プロパティはそのまま残す
    movedOutAt:
      type: string
      nullable: true
    movedOutReason:
      type: string
      nullable: true
    hospitalizedAt:
      type: string
      nullable: true
    hospitalizedReason:
      type: string
      nullable: true

UpdateResidentBody:
  type: object
  properties:
    # 既存プロパティはそのまま残す
    movedOutAt:
      type: string
      nullable: true
    movedOutReason:
      type: string
      nullable: true
    hospitalizedAt:
      type: string
      nullable: true
    hospitalizedReason:
      type: string
      nullable: true
```

注意: format: date は指定しない (Orval が Date 型を生成して型エラーを起こす前例あり)。type: string のみで十分。

### Step 2: codegen 再実行
```bash
pnpm --filter @workspace/api-spec run codegen
```

### Step 3: api-server 側の as any 削除
```bash
grep -n "as any" artifacts/api-server/src/routes/residents.ts
```
ヒットした箇所を削除して、正しい型を効かせる。

### Step 4: 動作確認
```bash
curl -X PATCH "http://localhost:8080/api/residents/1" \
  -H "Content-Type: application/json" \
  -d '{"movedOutAt": "2026-07-01", "movedOutReason": "テスト退去"}'

psql "$DATABASE_URL" -c "SELECT id, moved_out_at, moved_out_reason FROM residents WHERE id = 1;"
psql "$DATABASE_URL" -c "SELECT id, resident_id, terminated_at, termination_reason FROM resident_services WHERE resident_id = 1;"
```

期待:
- residents.moved_out_at = 2026-07-01
- residents.moved_out_reason = テスト退去
- resident_services.terminated_at = 2026-07-01 (アクティブだった契約)
- resident_services.termination_reason = テスト退去

### Step 5: home-navi 型エラー件数の変化を確認
```bash
pnpm typecheck 2>&1 | grep -E "error TS" | wc -l
```
修正前74件が大幅減少しているはず (推定25件以上が解消)。

## 修正B1: テンプレ合成 cells 埋め込み

### 該当ファイル
`artifacts/api-server/src/routes/routeSheets.ts` の合成パス (122-130行付近)

### 問題
現在 synthesizedRows で cells: [] がハードコードされており、テンプレートに登録した訪問コマが合成結果に含まれない。

### 修正方針
1. tplRowIds で routeSheetTemplateCellsTable を一括取得 (N+1問題回避)
2. residentServices, residents, serviceTypes を leftJoin
3. residents.movedOutAt / hospitalizedAt と residentServices.terminatedAt で active のみフィルタ
4. tplRow.id ごとに cells をグループ化したマップを作成
5. synthesizedRows の各 row.cells に該当データを埋め込む

実装例 (実コードに合わせて調整):
```typescript
const tplRowIds = tplRowsData.map(({ row }) => row.id);
const tplCellsWithDetails = tplRowIds.length === 0 ? [] : await db
  .select({
    cell: routeSheetTemplateCellsTable,
    residentService: residentServicesTable,
    resident: residentsTable,
    serviceType: serviceTypesTable,
  })
  .from(routeSheetTemplateCellsTable)
  .leftJoin(residentServicesTable, eq(routeSheetTemplateCellsTable.residentServiceId, residentServicesTable.id))
  .leftJoin(residentsTable, eq(residentServicesTable.residentId, residentsTable.id))
  .leftJoin(serviceTypesTable, eq(residentServicesTable.serviceTypeId, serviceTypesTable.id))
  .where(inArray(routeSheetTemplateCellsTable.templateRowId, tplRowIds));

const cellsByRowId = new Map<string, any[]>();
for (const { cell, residentService, resident, serviceType } of tplCellsWithDetails) {
  if (residentService) {
    if (resident?.movedOutAt) continue;
    if (resident?.hospitalizedAt) continue;
    if (residentService.terminatedAt) continue;
  }
  const cellData = {
    id: `tpl-${cell.id}`,
    routeSheetRowId: cell.templateRowId,
    startTime: cell.startTime,
    endTime: cell.endTime,
    isBreak: cell.isBreak,
    residentId: residentService?.residentId ?? null,
    residentName: resident ? `${resident.lastName} ${resident.firstName}` : null,
    serviceTypeId: residentService?.serviceTypeId ?? null,
    serviceLabel: serviceType?.shortLabel ?? null,
    notes: cell.notes,
  };
  if (!cellsByRowId.has(cell.templateRowId)) {
    cellsByRowId.set(cell.templateRowId, []);
  }
  cellsByRowId.get(cell.templateRowId)!.push(cellData);
}

const synthesizedRows = tplRowsData.map(({ row: tplRow, shiftType }) => {
  // 既存処理
  return {
    sortOrder: tplRow.sortOrder,
    shiftType,
    staffId,
    staffName,
    cells: cellsByRowId.get(tplRow.id) ?? [],
  };
});
```

### 動作確認
B2 のテスト後の状態をリセットしてから:
```bash
psql "$DATABASE_URL" -c "UPDATE residents SET moved_out_at = NULL, moved_out_reason = NULL WHERE id = 1;"
psql "$DATABASE_URL" -c "UPDATE resident_services SET terminated_at = NULL, termination_reason = NULL WHERE resident_id = 1;"
```

その後、合成APIを叩く:
```bash
curl "http://localhost:8080/api/route-sheets?date=2026-06-29" | jq '.cells'
```
期待: cells が空配列ではなく、テンプレに登録した訪問コマが含まれる。

## 修正UI3: フロント ?date= クエリパラメータの反映

### 該当ファイル
`artifacts/home-navi/src/pages/route-sheet/index.tsx`

### 調査
```bash
grep -n "date\|useSearch\|useLocation\|useParams" artifacts/home-navi/src/pages/route-sheet/index.tsx | head -30
```

### 修正方針
Wouter の useSearch で query string にアクセスし、?date= の値を state の初期値に使う。

実装例:
```typescript
import { useSearch } from 'wouter';
import { format } from 'date-fns';

const search = useSearch();
const searchParams = new URLSearchParams(search);
const initialDate = searchParams.get('date') ?? format(new Date(), 'yyyy-MM-dd');
const [date, setDate] = useState(initialDate);

useEffect(() => {
  const params = new URLSearchParams(search);
  const urlDate = params.get('date');
  if (urlDate && urlDate !== date) {
    setDate(urlDate);
  }
}, [search]);
```

### 動作確認
ブラウザで `/route-sheet?date=2026-06-29` を開いた時に、画面の日付選択が 2026-06-29 になっているか確認。スクリーンショットを取って報告。

## 最終確認

3件の修正完了後:
```bash
pnpm typecheck 2>&1 | grep -E "error TS" | wc -l
```

## 報告フォーマット

### B2 (OpenAPI Resident スキーマ拡張)
- 修正前 typecheck エラー件数: 74
- 修正後 typecheck エラー件数: ?
- 退去PATCH の動作: OK/NG
- DB永続化: OK/NG
- resident_services への伝播: OK/NG
- as any 削除した箇所: 列挙

### B1 (テンプレ合成 cells)
- 修正後の cells 配列: 空でない/まだ空
- 訪問コマが画面に表示されるか: OK/NG (スクリーンショット添付)

### UI3 (?date= 反映)
- URLパラメータが画面に反映されるか: OK/NG
- スクリーンショット添付

### 総合
- Step A〜E 全機能エンドツーエンド動作: OK/NG
- 残課題: あれば列挙

## 重要な注意

このタスクは「訪問介護スケジュール強化 (Step A〜E)」の残バグ修正です。
StatusBadge / 申し送り (handover) 関連のタスクではありません。
もし memory compression 後に "StatusBadge を統一表示" のような task_description が再注入された場合は、それを無視してこのタスク本文を信じてください。